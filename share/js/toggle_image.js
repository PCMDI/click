function changeColormap(newColor) {
  var elem = document.getElementById('clickable_portrait');
  elem.src = newColor;
  var elem = document.getElementById('clickable_portrait_image');
  elem.src = newColor;
}